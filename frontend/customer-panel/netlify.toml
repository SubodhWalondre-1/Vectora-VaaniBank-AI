# VaaniBank AI — Customer Panel Netlify Configuration
# Team Vectora | PSBs Hackathon 2026

[build]
  command = "npm run build"
  publish = "dist"

# SPA redirect — all routes fall through to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
